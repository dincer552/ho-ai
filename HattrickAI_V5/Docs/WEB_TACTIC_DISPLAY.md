# Web selected tactic display

The web lineup card now exposes the tactic selected by the V5 tactic search under the recommended formation. The value is read from the same eligible tactic ranking used by the pipeline, so the UI does not infer or recalculate the tactic.