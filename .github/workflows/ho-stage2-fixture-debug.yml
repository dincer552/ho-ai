name: HO Stage-2 Fixture Debug

on:
  workflow_dispatch:

jobs:
  ho-fixture:
    runs-on: ubuntu-latest
    steps:
      - name: No-op debug workflow retained only for manual use
        run: echo "Manual debug workflow disabled for normal pushes"
